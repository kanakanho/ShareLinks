import { FC } from "react";
import { styled } from "styled-components";

const MenuContainer = styled.div``;

const Menu: FC = () => {
  return (
    <MenuContainer>
      <div>text</div>
    </MenuContainer>
  );
};

export default Menu;
